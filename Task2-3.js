//TASK3
function power(a, b) {
    return a ** b; //raises the number a to the power of b (for example, 5 ** 3 = 125).
  }
 
  let a = prompt("Input number:")
  let b = prompt("input power of number:")
  
  console.log(power(Number(a), Number(b)))
  
  